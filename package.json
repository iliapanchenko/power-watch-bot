{
  "name": "power-watch-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram bot that watches devices by TCP probe and reports power outages",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "engines": {
    "node": ">=20"
  }
}
